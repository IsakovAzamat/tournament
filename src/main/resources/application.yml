spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/student_app
    username: postgres
    password: Azamat2003!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080